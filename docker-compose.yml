services:
  db:
    image: postgres:16
    container_name: hoj-db
    environment:
      POSTGRES_DB: hoj
      POSTGRES_USER: hoj
      POSTGRES_PASSWORD: hoj-local-password
    ports:
      - "5432:5432"
    volumes:
      - hoj-db-data:/var/lib/postgresql/data

volumes:
  hoj-db-data:
